
/// Ajout d'articles au panier

// Création d'un stockage sous la forme clé/valeur
localStorage.setItem('key', 'objectValue');

// Vérification du contenu du localStorage
const stockage = window.localStorage;
console.log(stockage);

// Récupération du contenu du localStorage
let valeurCle = localStorage.getItem('key');

// Actions à mener selon que le produit est ou n'est pas dans le panier
function addToCart() {
    if {
        // Le produit n'est pas dans le panier
    }
    else {
        // Le produit est déjà dans le panier
    }
}

// Récupération des éléments nécessaires
const imgKanap = img.src;
// getElementById ...

// Stockage des valeurs des produits dans un objet
let value = {
    key1 : value1,
    key2 : value2;
};

// Transformation de l'objet au format JSON
let objectValue = JSON.stringify(value)

// Transformation de l'objet au format javascript pour pouvoir l'utiliser
const objValue = JSON.parse(localStorage.getItem('key'));
console.log(objValue, "key"); 
